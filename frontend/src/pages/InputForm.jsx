import DailyInputForm from "../components/forms/DailyInputForm";

export default function InputPage() {
    return (
        <div style={{ padding: '2rem 0', minHeight: 'calc(100vh - 100px)' }}>
            <DailyInputForm />
        </div>
    );
}